export const routes = {
  app: {
    root: '/',
    home: {
      base: '/'
    },
    contact: {
      base: '/contact'
    }
  },
  dndTools: {
    root: '/dnd'
  }
};

export enum AppRoute {
  Home = '/',
  Contact = '/contact'
}
